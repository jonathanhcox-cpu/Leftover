# Leftover

Source for https://www.buildwithleftovers.com.

The `public` folder contains the production pages, calculators, styles and images.
Run `node build.cjs` to copy them into `dist` for deployment.
Vercel is connected to this repository and deploys the `main` branch.

Initial files were recovered from production deployment
`dpl_APwW59EadxksEBNykfTKKMoGLFe9` on 2026-09-08.
